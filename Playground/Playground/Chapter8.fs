module Playground.Chapter8

// Functionas are things
let plus3 x = x +3
let times2 x = x*2
let square = (fun x -> x*x)
let add3 = plus3



[<EntryPoint>]
let main argv =
    0 // return an integer exit code
